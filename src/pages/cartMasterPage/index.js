import React from "react";
import CommonPage from "../common";
import { CartMaster } from "../../pageComponents/cart";

const CartmasterPage = () => {
  return <CommonPage Children={<CartMaster />} />;
};

export default CartmasterPage;
